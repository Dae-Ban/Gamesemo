<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.GameInfoMapper">

<!-- 	<select id="selectTop10GamesByPlatform" resultType="gameinfo"> -->
<!-- 		SELECT * -->
<!-- 		FROM -->
<!-- 		( -->
<!-- 		SELECT * -->
<!-- 		FROM GAME_INFO -->
<!-- 		WHERE -->
<!-- 		LOWER(GI_PLATFORM) = LOWER(#{platform}) -->
<!-- 		ORDER BY GI_NUM -->
<!-- 		) -->
<!-- 		WHERE ROWNUM -->
<!-- 		&lt;= 10 -->
<!-- 	</select> -->

	<select id="getGameInfo" parameterType="map"
		resultType="gameinfo">
		SELECT *
		FROM GAME_INFO
		WHERE G_NUM = #{gnum}
		AND GI_STATE =
		#{state}
	</select>

	<select id="getGameInfosByGnum" parameterType="int"
		resultType="gameinfo">
		SELECT *
		FROM GAME_INFO
		WHERE G_NUM = #{gnum}
		ORDER BY
		GI_STATE, GI_DATE DESC
	</select>

	<select id="getGameById" parameterType="String"
		resultType="gameinfo">
		SELECT *
		FROM GAME_INFO
		WHERE G_NUM = #{gnum}
		ORDER BY
		GI_FPRICE ASC
		FETCH FIRST 1 ROWS ONLY
	</select>

	<select id="getPlatforms" resultType="gameinfo">
		SELECT GI_NUM, G_NUM,
		GI_PLATFORM, GI_TITLE, GI_THUMB, GI_PRICE, GI_FPRICE,
		GI_LINK
		FROM
		GAME_INFO
		WHERE G_NUM = #{gnum}
		ORDER BY GI_FPRICE ASC
	</select>


	<select id="getReviewVideos" parameterType="String"
		resultType="gameinfo">
		SELECT *
		FROM GAME_INFO
		WHERE G_NUM = #{gnum}
		ORDER BY
		GI_FPRICE ASC
		FETCH FIRST 1 ROWS ONLY
	</select>

	<select id="getByGame" parameterType="int"
		resultType="com.example.demo.model.GameInfo">
		SELECT
		G_NUM,
		GI_PLATFORM,
		GI_PRICE,
		GI_FPRICE,
		GI_RATE,
		GI_LINK,
		GI_DATE,
		GI_THUMB,
		GI_TITLE
		FROM GAME_INFO
		WHERE G_NUM = #{gnum}
		ORDER BY GI_DATE DESC
	</select>
</mapper>