<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.GameInfoMapper">
	<insert id="insertGameInfo" parameterType="gameinfo">
		<selectKey keyProperty="giNum" order="BEFORE" resultType="int">
			SELECT NVL(MAX(gi_num), 0)+1 FROM game_info
		</selectKey>
		INSERT INTO game_info VALUES(${giNum},#{gNum},#{giPlatform},#{giTitle},
		#{giThumb},#{giPrice},#{giFprice},#{giRate},#{giLink},#{giState},#{giDate})
	</insert>
	<select id="gameInfo">
		SELECT * FROM game_info ORDER BY gi_num ASC;
	</select>
</mapper>