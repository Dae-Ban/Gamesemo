<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.MemberMapper">
	<select id="selectAllEmailSubscribers"
		resultType="com.example.demo.model.Member">
		SELECT * FROM MEMBER WHERE EMAIL_AD = 'Y'
	</select>
	
	<!-- 회원가입 -->
	<insert id="insertMember"
		parameterType="com.example.demo.model.Member">
		INSERT INTO member (
		id, name, nickname, pw, birth_date,
		email_id, email_domain,
		phone, gender,
		join_date, state, email_ad,
		email_verified
		) VALUES (
		#{id}, #{name}, #{nickname}, #{pw},
		#{birthDate},
		#{emailId}, #{emailDomain},
		#{phone}, #{gender},
		SYSDATE,
		0, #{emailAd}, 'n'
		)
	</insert>
	
	<!-- 닉네임 중복 확인 -->
	<select id="countByNickname" parameterType="String"
		resultType="int">
		SELECT COUNT(*) FROM member WHERE nickname = #{nickname}
	</select>

	<select id="findByEmail" resultType="com.example.demo.model.Member">
		SELECT * FROM member
		WHERE email_id || '@' || email_domain = #{email}
	</select>
	
	<select id="findById" parameterType="String"
		resultType="com.example.demo.model.Member">
		SELECT * FROM member WHERE id = #{id}
	</select>
	
	<!-- (1) 이메일 중복 확인 -->
	<select id="selectByEmailForRegister" parameterType="string" resultType="com.example.demo.model.Member">
		SELECT * FROM member WHERE (email_id || '@' || email_domain) = #{email}
	</select>
	
	<!-- 회원정보 수정 -->
	<update id="updateMember"
		parameterType="com.example.demo.model.Member">
		UPDATE member
		SET
		name = #{name},
		nickname = #{nickname},
		phone =
		#{phone},
		email_id = #{emailId},
		email_domain = #{emailDomain},
		gender =
		#{gender}
		<if test="birthDate != null and birthDate != ''">
			, birth_date = TO_DATE(#{birthDate}, 'YYYY-MM-DD')
		</if>
		WHERE id = #{id}
	</update>
	
	<!-- 비밀번호 변경 -->
	<update id="updatePassword"
		parameterType="com.example.demo.model.Member">
		UPDATE member
		SET pw = #{pw}
		WHERE id = #{id}
	</update>

	<!-- 비번찾기에서 비번 번경 -->
	<update id="updatePasswordForFind" parameterType="map">
		UPDATE member
		SET pw = #{newPw}
		WHERE id = #{id}
	</update>
	
	<!-- 회원 탈퇴 : 실제 DB삭제 안하고 상태값만 바꿈 -->
	<update id="deleteMember" parameterType="String">
		UPDATE member
		SET state
		=
		1
		WHERE id = #{id}
		AND state = 0
	</update>
	
	<select id="checkIdExists" parameterType="String" resultType="int">
		SELECT
		COUNT(*) FROM member WHERE id = #{id}
	</select>
		
	<select id="findByEmailForRegister" parameterType="string"
		resultType="com.example.demo.model.Member">
		SELECT * FROM member WHERE email_id || '@' || email_domain
		= #{email}
	</select>
	
	<!--회원가입-->
	<insert id="registerMember"
		parameterType="com.example.demo.model.Member">
		INSERT INTO MEMBER (
		ID, NAME, NICKNAME, PW, BIRTH_DATE,
		EMAIL_ID, EMAIL_DOMAIN,
		SOCIAL_PLATFORM, SOCIAL_ID,
		PHONE,
		GENDER,
		EMAIL_AD, EMAIL_VERIFIED,
		JOIN_DATE, STATE
		)
		VALUES (
		#{id},
		#{name},
		#{nickname}, #{pw}, #{birthDate},
		#{emailId}, #{emailDomain},
		#{socialPlatform, jdbcType=VARCHAR}, #{socialId, jdbcType=VARCHAR},
		#{phone}, #{gender},
		#{emailAd}, #{emailVerified},
		SYSDATE, #{state}
		)
	</insert>
	
	<!-- 모달 로그인 -->
	<select id="modalLogin" parameterType="map" resultType="com.example.demo.model.Member">
		SELECT * FROM MEMBER WHERE ID = #{id} AND PW = #{pw}
	</select>
	
	<!-- 영교님 부분 -->
	
	 <select id="login" parameterType="String" resultType="member">
        SELECT *
        FROM MEMBER
        WHERE ID = #{id}
    </select>
    
    <select id="findBySocialIdAndPlatform" resultType="member">
        SELECT * FROM MEMBER WHERE SOCIAL_ID = #{socialId} AND SOCIAL_PLATFORM = #{platform}
    </select>
</mapper>


